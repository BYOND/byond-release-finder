apiVersion: v1
kind: Pod
spec:
  containers:
    - name: bash
      image: ellerbrock/alpine-bash-curl-ssl:latest
      volumeMounts:
        - name: byond-upstream-releases
          mountPath: /releases
  securityContext:
    fsGroup: 1000
  volumes:
    - name: byond-upstream-releases
      persistentVolumeClaim:
        claimName: byond-upstream-releases
